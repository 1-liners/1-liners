/**
 * @module 1-liners/not
 * 
 * Same as `!a`.
 * 
 * @example
 * 
 * 	var not = require('1-liners/not');
 * 
 * 	not(true); // => false
 * 	not(false); // => true
 * 
 **/
export default (a) => !a;
