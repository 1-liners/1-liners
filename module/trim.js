/**
 * @module 1-liners/trim
 *
 * @description
 *
 * Same as `'STR'.trim()`.
 *
 * @example
 *
 * 	const trim = require('1-liners/trim');
 *
 * 	trim('  super  ') // => super
 *
 */
export default (str) => str.trim();
