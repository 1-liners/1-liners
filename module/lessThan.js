/**
 * @module 1-liners/lessThan
 *
 * @description
 *
 * Same as `a < b`.
 *
 * @example
 *
 * 	const lessThan = require('1-liners/lessThan');
 *
 * 	lessThan(1, 2); // => true
 * 	lessThan(1, 1); // => false
 * 	lessThan(2, 1); // => false
 *
 */
export default (x, y) => x < y;
